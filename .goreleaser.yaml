version: 2

builds:
  - main: ./cmd/repohealth
    binary: repohealth
    ldflags:
      - -s -w -X github.com/spbuilds/repohealth/internal/cli.Version={{.Version}} -X github.com/spbuilds/repohealth/internal/cli.Commit={{.ShortCommit}} -X github.com/spbuilds/repohealth/internal/cli.Date={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^style:"
      - "^chore:"
